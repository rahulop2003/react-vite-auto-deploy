version: 0.2

phases:
  install:
    commands:
      - curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
      - yum install -y nodejs
      - node -v
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  base-directory: dist
  files:
    - '**/*'